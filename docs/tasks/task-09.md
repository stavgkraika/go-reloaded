# Task 9: Execute (hex) Command

## Description
Convert the previous hexadecimal word to its decimal equivalent when (hex) command is encountered.

## Test First
**TestHexConversion**: Test hexadecimal to decimal conversion

## Implementation Goal
Implement applyHexConversion() function that converts hex strings to decimal numbers.

## Validation
Numeric conversion results are valid and accurate.

## Acceptance Criteria
- [ ] "1E (hex)" becomes "30"
- [ ] "FF (hex)" becomes "255"
- [ ] "A (hex)" becomes "10"
- [ ] Invalid hex strings handled gracefully
- [ ] Case insensitive hex conversion
- [ ] Large hex numbers converted correctly
- [ ] Tests pass for various hex values

## Phase
Phase 3 – Command State (Transformations)