# Task 11: Execute (up), (low), (cap) Commands

## Description
Modify the previous word's case using (up) for uppercase, (low) for lowercase, and (cap) for capitalize.

## Test First
**TestCaseTransformationSingle**: Test single word case transformations

## Implementation Goal
Implement applyCaseTransformation() function with count=1 for single word transformations.

## Validation
Correct capitalization is applied to the target word.

## Acceptance Criteria
- [ ] "hello (up)" becomes "HELLO"
- [ ] "WORLD (low)" becomes "world"
- [ ] "example (cap)" becomes "Example"
- [ ] Mixed case words handled correctly
- [ ] Special characters and numbers preserved
- [ ] Empty or invalid words handled gracefully
- [ ] Tests pass for all case transformation types

## Phase
Phase 3 – Command State (Transformations)